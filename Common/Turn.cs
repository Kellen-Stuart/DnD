using System.Data.SqlTypes;
using Common.Abstractions.Character;

namespace Common
{
    public class Turn
    {
        private Alliance Alliance { get; private set; }
        
        public Turn(Character character)
        {
            
        }
    }
}